{
  "name": "sdms-ledger-gateway",
  "version": "1.0.0",
  "description": "REST gateway between the SDMS backend and the Hyperledger Fabric network",
  "main": "src/server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.12.0",
    "@hyperledger/fabric-gateway": "^1.7.0",
    "@hyperledger/fabric-protos": "^0.3.0",
    "express": "^4.21.0"
  }
}
