[pytest]
testpaths = tests
pythonpath = . tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    fabric: needs the Hyperledger Fabric network from fabric/docker-compose.yml to be running
    ai: needs the local AI service (docker run sdms-ai) and, for summaries, Ollama
